import fetch from 'node-fetch'; // Import fetch if necessary for your environment

// put the copied requests from browsere here. 
// Wrap it in String.raw`{fetchRequest}` for reverse engineering data and request body

// EXAMPLE
// const req_arr = [
//     String.raw`fetch(url, requestOptions)`,
//     String.raw`fetch(url, requestOptions)`,
//     String.raw`fetch(url, requestOptions)`
// ];

const req_arr = [
    
];

export default req_arr;
